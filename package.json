{
  "name": "autofilm",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "mkdir -p dist && cp -r frontend/* dist/ && node serve.cjs",
    "build": "npx vite build"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.49.4",
    "vite": "^8.0.8"
  }
}
