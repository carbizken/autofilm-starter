{
  "name": "autofilm",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "mkdir -p dist && cp -r frontend/* dist/",
    "build": "mkdir -p dist && cp -r frontend/* dist/"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.49.4"
  }
}
